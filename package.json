{
  "name": "stillness",
  "private": true,
  "description": "Stillness journal — static index.html app (Vercel deploy). Use npm run build for CI sanity check.",
  "scripts": {
    "build": "node -e \"const fs=require('fs'); if(!fs.existsSync('index.html')){process.exit(1)} console.log('Stillness: index.html present — build OK');\""
  }
}
